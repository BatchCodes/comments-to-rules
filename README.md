# Comments to Rules

A Claude Code skill. It turns a GitHub repo's PR review-comment history into
per-language coding-style rule files, adds rules inferred from the local
codebase, and can then run a review against the result.